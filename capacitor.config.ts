import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.shunchaaaki.profitcalculator",
  appName: "Profit Calculator",
  webDir: "dist/public",
  bundledWebRuntime: false,
};

export default config;
